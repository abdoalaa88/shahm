# Design references

`stitch/` holds the exported screen designs (`code.html` + `screen.png`) and
`stitch/shahm/DESIGN.md` (the colour/typography tokens). They are for reference
only and are not part of the build.

| Folder | Screen |
| --- | --- |
| `welcomescreen` | Welcome |
| `signupscreen` | Sign up |
| `roleselectionscreen` | Role selection |
| `requesterdashboard` | Requester dashboard |
| `_1` … `_8` | Additional exported screens |
