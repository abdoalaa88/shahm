import React from 'react';

/** The app's guiding quote, shown at the top of the main content on the home tabs. */
export const QuoteCard: React.FC = () => (
  <div className="stitch-soft-card px-4 py-3 text-center">
    <p className="text-sm font-semibold leading-8 text-[#005131]">
      «من سار بين الناس جابراً للخواطر أدركه الله في جوف المخاطر.»
    </p>
  </div>
);
